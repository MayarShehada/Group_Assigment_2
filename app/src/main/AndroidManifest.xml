<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.birzeit.group_assignment_2">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Group_Assignment_2">
        <activity android:name=".Activities.BillActivity"></activity>
        <activity android:name=".Activities.CartListActivity" />
        <activity android:name=".Activities.CategoryListActivity" />
        <activity android:name=".Activities.ItemListActivity" />
        <activity android:name=".Activities.DetailesShoeActivity" />
        <activity android:name=".Activities.HomePageActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>